---
import BaseLayout from '../layouts/BaseLayout.astro';
import TrustLogos from '../components/TrustLogos.astro';
import CustomerReviews from '../components/CustomerReviews.astro';

const pageTitle = "Parlons de votre projet | Hercules Merchandising France";
const pageDescription = "Contactez Hercules Merchandising pour votre merchandising sportif personnalisé. Service de design gratuit, réponse rapide, accompagnement de l'idée à la livraison.";
---

<BaseLayout
  title={pageTitle}
  description={pageDescription}
  pageType="page"
>
  <!-- Hero Section -->
  <section class="hero-section">
    <div class="hero-container">
      <div class="hero-text">
        <h1>Parlons de votre projet</h1>
        <p class="hero-intro">Vous avez une question ou souhaitez démarrer un projet ?</p>
        <p class="hero-intro">Envoyez-nous un message et nous vous répondrons rapidement, généralement dans la journée.</p>
        <p class="hero-intro">Que vous ayez déjà une idée précise ou que vous souhaitiez simplement explorer vos options, nous vous guiderons tout au long du processus.</p>
      </div>
    </div>
  </section>

  <!-- Pourquoi nous contacter -->
  <section class="why-section">
    <div class="container">
      <h2 class="section-title">Pourquoi nous contacter</h2>
      <div class="why-grid">
        <div class="why-card">
          <div class="why-icon">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#10C99E" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 19l7-7 3 3-7 7-3-3z"></path>
              <path d="M18 13l-1.5-7.5L2 2l3.5 14.5L13 18l5-5z"></path>
              <path d="M2 2l7.586 7.586"></path>
              <circle cx="11" cy="11" r="2"></circle>
            </svg>
          </div>
          <p>Service de design gratuit et sans engagement</p>
        </div>
        <div class="why-card">
          <div class="why-icon">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#10C99E" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
            </svg>
          </div>
          <p>Réponse rapide et communication claire</p>
        </div>
        <div class="why-card">
          <div class="why-icon">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#10C99E" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 11l3 3L22 4"></path>
              <path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"></path>
            </svg>
          </div>
          <p>Accompagnement de l'idée à la livraison</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact Form Section -->
  <section class="form-section">
    <div class="container">
      <h2 class="section-title">Votre message</h2>
      <div class="form-wrapper">
        <form class="contact-form" id="contact-form" novalidate>
          <!-- Name + Email Row -->
          <div class="form-row">
            <div class="form-field">
              <label for="contact-name">Nom <span class="required">*</span></label>
              <input type="text" id="contact-name" name="name" placeholder="Votre nom" required />
            </div>
            <div class="form-field">
              <label for="contact-email">E-mail <span class="required">*</span></label>
              <input type="email" id="contact-email" name="email" placeholder="Votre e-mail" required />
            </div>
          </div>

          <!-- Organisation -->
          <div class="form-field">
            <label for="contact-organisation">Organisation <span class="optional">(facultatif)</span></label>
            <input type="text" id="contact-organisation" name="organisation" placeholder="Club, équipe, entreprise ou organisation" />
          </div>

          <!-- Message -->
          <div class="form-field">
            <label for="contact-message">Message <span class="required">*</span></label>
            <textarea id="contact-message" name="message" placeholder="Parlez-nous de votre projet..." rows="6" required></textarea>
          </div>

          <!-- File Upload -->
          <div class="form-field">
            <label for="contact-files">Joindre un fichier <span class="optional">(facultatif)</span></label>
            <input type="file" id="contact-files" name="files" multiple accept="image/*,.pdf,.doc,.docx,.xls,.xlsx" />
            <span class="file-hint">Logos, designs ou images de référence</span>
          </div>

          <!-- Error / Success Messages -->
          <div class="form-message" id="form-message"></div>

          <!-- Submit Button -->
          <div class="form-submit">
            <button type="submit" id="contact-submit">Envoyer le message</button>
          </div>
        </form>
      </div>
    </div>
  </section>

  <!-- Ce qui se passe ensuite -->
  <section class="next-section">
    <div class="container">
      <h2 class="section-title">Ce qui se passe ensuite</h2>
      <p class="section-intro">Une fois que vous nous avez contactés :</p>
      <div class="steps-grid">
        <div class="step">
          <span class="step-number">1</span>
          <p>Nous examinons votre demande</p>
        </div>
        <div class="step">
          <span class="step-number">2</span>
          <p>Nous vous répondons rapidement</p>
        </div>
        <div class="step">
          <span class="step-number">3</span>
          <p>Nous discutons de votre projet</p>
        </div>
        <div class="step">
          <span class="step-number">4</span>
          <p>Nous créons votre premier design gratuitement</p>
        </div>
      </div>
      <p class="no-pressure">Vous décidez si vous souhaitez continuer — sans pression.</p>
    </div>
  </section>

  <!-- Trusted Section -->
  <section class="trust-section">
    <div class="container">
      <h2 class="section-title">La confiance d'organisations à travers l'Europe</h2>
      <p class="section-intro">Nous accompagnons plus de 1 000 clients chaque année.</p>
      <p class="section-intro">Pour un avis honnête sur notre façon de travailler, nous vous invitons à consulter nos <a href="https://www.google.com/maps/place/Hercules+Merchandising+France/@30.886403,-49.4022061,3z/data=!3m1!4b1!4m8!3m7!1s0x21d159209e369b9d:0xa126617f91835893!8m2!3d30.886403!4d-49.4022062!9m1!1b1!16s%2Fg%2F11vkhhwqmm?entry=ttu" target="_blank" rel="noopener noreferrer">avis Google</a>.</p>
    </div>
  </section>

  <!-- Trust Logos -->
  <TrustLogos />

  <!-- Avis clients -->
  <CustomerReviews />
</BaseLayout>

<style>
  /* Hero Section */
  .hero-section {
    background-color: #444444;
    background-image: url('/images/about-hero-banner.jpg');
    background-size: cover;
    background-position: 50% 0%;
    background-repeat: no-repeat;
    position: relative;
    min-height: 450px;
    display: flex;
    align-items: center;
  }

  .hero-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(286deg, rgba(0, 0, 0, 0) 0%, rgb(0, 0, 0) 100%);
    opacity: 0.6;
  }

  .hero-container {
    position: relative;
    max-width: 1280px;
    margin: 0 auto;
    padding: 60px 40px;
    width: 100%;
  }

  .hero-text {
    max-width: 650px;
  }

  h1 {
    font-family: 'Jost', sans-serif;
    font-size: 52px;
    font-weight: 600;
    color: #FFFFFF;
    text-transform: uppercase;
    line-height: 1.1;
    margin: 0 0 20px;
  }

  .hero-intro {
    font-family: 'Roboto', sans-serif;
    font-size: 17px;
    line-height: 1.7;
    color: rgba(255, 255, 255, 0.9);
    margin: 0 0 10px;
  }

  /* Shared */
  .container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 40px;
  }

  .section-title {
    font-family: 'Jost', sans-serif;
    font-size: 35px;
    font-weight: 600;
    color: #253461;
    text-transform: uppercase;
    text-align: center;
    margin: 0 0 15px;
  }

  .section-intro {
    font-family: 'Roboto', sans-serif;
    font-size: 17px;
    line-height: 1.7;
    color: #5F5F5F;
    text-align: center;
    max-width: 750px;
    margin: 0 auto 10px;
  }

  .section-intro a {
    color: #469ADC;
    text-decoration: underline;
  }

  .section-intro a:hover {
    color: #253461;
  }

  /* Why Contact Us */
  .why-section {
    padding: 70px 0;
    background: #FFFFFF;
  }

  .why-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
    margin-top: 40px;
  }

  .why-card {
    text-align: center;
    padding: 30px 20px;
    background: #FAFAFA;
    border-radius: 15px;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .why-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
  }

  .why-icon {
    margin-bottom: 15px;
  }

  .why-card p {
    font-family: 'Jost', sans-serif;
    font-size: 18px;
    font-weight: 500;
    color: #253461;
    margin: 0;
  }

  /* Form Section */
  .form-section {
    padding: 70px 0;
    background: #FAFAFA;
  }

  .form-wrapper {
    max-width: 750px;
    margin: 30px auto 0;
    background: #FFFFFF;
    border-radius: 15px;
    padding: 40px;
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .form-row {
    display: flex;
    gap: 20px;
  }

  .form-row .form-field {
    flex: 1;
  }

  .form-field {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .form-field label {
    font-family: 'Jost', sans-serif;
    font-size: 15px;
    font-weight: 600;
    color: #253461;
  }

  .required {
    color: #e74c3c;
  }

  .optional {
    font-weight: 400;
    color: #999;
    font-size: 13px;
  }

  .form-field input[type="text"],
  .form-field input[type="email"],
  .form-field input[type="tel"],
  .form-field textarea {
    font-family: 'Jost', sans-serif;
    font-size: 14px;
    padding: 12px 15px;
    border: 1px solid #DCDCDC;
    border-radius: 8px;
    background: #fff;
    color: #253461;
    width: 100%;
    box-sizing: border-box;
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  .form-field input:focus,
  .form-field textarea:focus {
    outline: none;
    border-color: #10C99E;
    box-shadow: 0 0 0 3px rgba(16, 201, 158, 0.1);
  }

  .form-field input::placeholder,
  .form-field textarea::placeholder {
    color: #8D8D8D;
  }

  .form-field textarea {
    resize: vertical;
    min-height: 140px;
  }

  .form-field input[type="file"] {
    font-family: 'Jost', sans-serif;
    font-size: 14px;
    color: #253461;
  }

  .file-hint {
    font-family: 'Roboto', sans-serif;
    font-size: 13px;
    color: #999;
  }

  .form-message {
    display: none;
    padding: 12px 15px;
    border-radius: 8px;
    font-family: 'Jost', sans-serif;
    font-size: 14px;
  }

  .form-message.success {
    display: block;
    background: #f0fdf4;
    border: 1px solid #bbf7d0;
    color: #15803d;
  }

  .form-message.error {
    display: block;
    background: #fef2f2;
    border: 1px solid #fecaca;
    color: #dc2626;
  }

  .form-submit {
    display: flex;
    justify-content: center;
    margin-top: 10px;
  }

  .form-submit button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 15px 50px;
    background: #10C99E;
    border: 1px solid #10C99E;
    border-radius: 50px;
    color: #fff;
    font-family: 'Jost', sans-serif;
    font-size: 15px;
    font-weight: 500;
    text-transform: uppercase;
    cursor: pointer;
    transition: all 0.3s ease;
    min-width: 180px;
  }

  .form-submit button:hover:not(:disabled) {
    background: transparent;
    color: #10C99E;
  }

  .form-submit button:disabled {
    opacity: 0.7;
    cursor: not-allowed;
  }

  /* What Happens Next */
  .next-section {
    padding: 70px 0;
    background: #FFFFFF;
  }

  .steps-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 25px;
    margin-top: 30px;
  }

  .step {
    text-align: center;
    padding: 25px 15px;
  }

  .step-number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: #10C99E;
    color: #FFFFFF;
    font-family: 'Jost', sans-serif;
    font-size: 22px;
    font-weight: 600;
    margin-bottom: 15px;
  }

  .step p {
    font-family: 'Roboto', sans-serif;
    font-size: 15px;
    line-height: 1.5;
    color: #5F5F5F;
    margin: 0;
  }

  .no-pressure {
    font-family: 'Roboto', sans-serif;
    font-size: 16px;
    color: #253461;
    font-weight: 500;
    text-align: center;
    margin-top: 25px;
  }

  /* Trust Section */
  .trust-section {
    padding: 70px 0 30px;
    background: #FAFAFA;
  }

  /* Mobile Responsive */
  @media (max-width: 1024px) {
    .why-grid {
      grid-template-columns: 1fr;
    }

    .steps-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .hero-section {
      min-height: auto;
    }

    .hero-container {
      padding: 40px 20px;
    }

    h1 {
      font-size: 32px;
    }

    .hero-intro {
      font-size: 15px;
    }

    .container {
      padding: 0 20px;
    }

    .section-title {
      font-size: 24px;
    }

    .why-section,
    .form-section,
    .next-section,
    .trust-section {
      padding: 45px 0;
    }

    .form-wrapper {
      padding: 25px 20px;
    }

    .form-row {
      flex-direction: column;
    }

    .steps-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
    }

    .step {
      padding: 15px 10px;
    }
  }
</style>

<script>
  import { pushGenerateLead } from '../lib/tracking';

  document.addEventListener('DOMContentLoaded', () => {
    const form = document.getElementById('contact-form') as HTMLFormElement;
    const submitBtn = document.getElementById('contact-submit') as HTMLButtonElement;
    const messageDiv = document.getElementById('form-message') as HTMLDivElement;

    if (form) {
      form.addEventListener('submit', async (e) => {
        e.preventDefault();

        submitBtn.disabled = true;
        submitBtn.textContent = 'Envoi en cours...';
        messageDiv.className = 'form-message';
        messageDiv.textContent = '';

        try {
          const formData = new FormData(form);
          formData.append('date', new Date().toLocaleDateString('fr-FR'));
          formData.append('time', new Date().toLocaleTimeString('fr-FR', { hour: '2-digit', minute: '2-digit' }));
          formData.append('pageTitle', document.title);
          formData.append('pageUrl', window.location.href);
          formData.append('referrer', document.referrer || '(direct)');
          formData.append('userAgent', navigator.userAgent);

          // Handle file input
          const fileInput = document.getElementById('contact-files') as HTMLInputElement;
          if (fileInput && fileInput.files) {
            for (let i = 0; i < fileInput.files.length; i++) {
              formData.append(`file_${i}`, fileInput.files[i]);
            }
          }

          const response = await fetch('/api/contact', {
            method: 'POST',
            body: formData,
          });

          if (!response.ok) {
            throw new Error('Submission failed');
          }

          pushGenerateLead('contact');

          messageDiv.className = 'form-message success';
          messageDiv.textContent = 'Merci pour votre message ! Nous vous répondrons rapidement, généralement dans la journée.';
          form.reset();
        } catch (error) {
          messageDiv.className = 'form-message error';
          messageDiv.textContent = 'Une erreur est survenue. Veuillez réessayer.';
        } finally {
          submitBtn.disabled = false;
          submitBtn.textContent = 'Envoyer le message';
        }
      });
    }
  });
</script>
